#include <stdio.h>
#include "main.h"

/**
 * main - Prints file name
 * @argc: argument count
 * @argv: Argument vector
 * Return: File name
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
